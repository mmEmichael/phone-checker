# Конфигурация Phone-service (воркер проверки номеров)

# Подключение к Redis (очередь задач и хранение результатов)
redis:
  host: localhost
  port: 6379

# Очередь задач: имя списка Redis, из которого забираются task_id.
# Должно совпадать с именем очереди в gateway.
queue:
  name: tasks
